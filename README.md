# MongoDB

- If you wish, change the user and password in environment list of `docker-compose.yml`

- Start mongodb server in detached mode with `docker compose up -d`

- MongoDB will bind default port 27017 of localhost

- See logs with `docker compose logs -f`
